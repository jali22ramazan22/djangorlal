# Как использовать JWT аутентификацию в Swagger UI

## Шаг 1: Открыть Swagger UI

Откройте браузер и перейдите по адресу:
```
http://localhost:8000/api/v1/docs/
```

## Шаг 2: Получить JWT токен

### Вариант А: Через Swagger UI (рекомендуется)

1. Найдите эндпоинт **POST /api/v1/auth/users/login**
2. Нажмите "Try it out"
3. Введите данные для входа:
   ```json
   {
     "email": "admin@example.com",
     "password": "admin123"
   }
   ```
4. Нажмите "Execute"
5. В ответе получите токены:
   ```json
   {
     "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
     "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
   }
   ```
6. **Скопируйте значение `access` токена** (это ваш JWT токен)

### Вариант Б: Через curl

```bash
curl -X POST http://localhost:8000/api/v1/auth/users/login \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@example.com", "password": "admin123"}'
```

## Шаг 3: Авторизоваться в Swagger UI

1. В правом верхнем углу Swagger UI найдите кнопку **"Authorize"** (или иконку замка 🔒)
2. Нажмите на неё
3. В открывшемся модальном окне увидите поле **"bearerAuth (http, Bearer)"**
4. В поле "Value" введите **только токен** (БЕЗ слова "Bearer"):
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ0b2tlbl90eXBlIjoiYWNjZXNzIiwiZXhwIjoxNzM...
   ```

   ⚠️ **ВАЖНО:** Вставляйте ТОЛЬКО сам токен, НЕ добавляйте "Bearer " в начало!

5. Нажмите **"Authorize"**
6. Нажмите **"Close"**

## Шаг 4: Тестировать API с аутентификацией

Теперь все запросы будут автоматически включать JWT токен в заголовке:
```
Authorization: Bearer <ваш_токен>
```

### Примеры тестирования permissions:

#### 1. Получить список задач (только из проектов пользователя)
- **GET /api/v1/tasks/**
- Должны увидеть только задачи из проектов, где вы - участник

#### 2. Получить список проектов (только свои)
- **GET /api/v1/projects/**
- Должны увидеть только проекты, где вы - участник

#### 3. Попробовать изменить чужой проект (должно быть 403 Forbidden)
- Создайте проект как admin
- Войдите как regular user (другой токен)
- Попробуйте **PUT /api/v1/projects/{id}/**
- Должны получить **403 Forbidden**

#### 4. Создать задачу в своем проекте (должно работать)
- **POST /api/v1/tasks/**
- Укажите project_id вашего проекта
- Должен вернуться **201 Created**

## Шаг 5: Проверка истечения токена

JWT access токен живёт **15 минут** (настройка в settings).

Когда токен истечёт, получите **401 Unauthorized**. В этом случае:

1. Используйте **refresh токен** для получения нового access токена
2. Эндпоинт: **POST /api/v1/auth/users/refresh**
3. Тело запроса:
   ```json
   {
     "refresh": "ваш_refresh_токен"
   }
   ```
4. Получите новый access токен
5. Обновите авторизацию в Swagger UI

## Доступные тестовые пользователи

После запуска `python manage.py generate_all_data`:

### Администратор (для всех действий)
- Email: `admin@example.com`
- Password: `admin123`

### Обычные пользователи (для тестирования permissions)
- Email: любой сгенерированный email (смотрите вывод команды)
- Password: `testpass123`

## Проверка работы permissions

### ✅ Что ДОЛЖНО работать:
- Просмотр своих проектов
- Просмотр задач из своих проектов
- Создание задач в своих проектах
- Изменение задач, где вы assignee
- Просмотр компаний

### ❌ Что НЕ должно работать (403 Forbidden):
- Просмотр чужих проектов (не член проекта)
- Изменение чужих проектов (не автор)
- Изменение задач, где вы не assignee
- Изменение компаний (не admin)

## Удаление авторизации

Чтобы выйти из системы в Swagger UI:
1. Нажмите кнопку **"Authorize"**
2. Нажмите **"Logout"**
3. Нажмите **"Close"**

Теперь все запросы будут возвращать **401 Unauthorized**.

## Troubleshooting

### Проблема: Всё равно 401 Unauthorized после авторизации
**Решение:** Убедитесь, что:
- Вставили ТОЛЬКО токен, без "Bearer "
- Токен не истёк (15 минут)
- Нажали "Authorize" и "Close"

### Проблема: Токен невалидный
**Решение:** Получите новый токен через /api/v1/auth/users/login

### Проблема: 403 Forbidden вместо 200 OK
**Решение:** Это корректное поведение! Проверьте:
- Вы член проекта? (для просмотра)
- Вы автор проекта? (для изменения)
- Вы assignee задачи? (для изменения)

## Технические детали

### Формат JWT токена в запросе:
```
GET /api/v1/tasks/ HTTP/1.1
Host: localhost:8000
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Время жизни токенов (settings/conf.py):
- Access token: 15 минут
- Refresh token: 7 дней

### Что содержит JWT токен:
```json
{
  "token_type": "access",
  "exp": 1733079600,
  "iat": 1733078700,
  "jti": "abc123...",
  "user_id": 1
}
```
